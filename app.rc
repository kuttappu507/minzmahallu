// Windows application resources.
// Embed the Per-Monitor-V2 application manifest so Windows establishes the
// correct DPI awareness before Qt creates the application object.
1 RT_MANIFEST "app.manifest"
